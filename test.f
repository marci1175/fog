import print(str: string): int;
import getchar(): int;

struct test {
    inner: int,
}

struct string_wrap {
    inner: string,
    count: uint,
}

function main(): int {
    print("Hello world! :3");

    getchar();

    return 0;
}