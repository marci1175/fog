function sum(rhs: int, lhs: int): int {
    int result = rhs + lhs;

    return result;
}

function main(): int {
    float x = -2.34;

    int y = 3;
    int z = 2 + 5 + 4 + 2;

    sum(rhs = z, lhs = y);

    return 0;
}