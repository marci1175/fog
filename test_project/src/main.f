import print(msg: string): int;

struct test {
    name: string,
    age: int,
    igen: float,
    misc: string,
}

function main(): int {
    test marci = test { name: "Marci", age: 32, igen: 23.2, misc: "agh", };
 
    return 0;
}