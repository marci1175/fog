import printf(str: string, res: int): int;

function main(): int {
    int a = 0;

    loop {
        a = 23 - 2;
               
        printf("Krisztian eletkora: %i\n", a * 2);
    }

    return 0;
}
