import printf(msg: string, num: int): int;

function main(): int {
    int a = 0;

    loop {
        a = 23 - 2 + 1 * 3 + 2 - 435 + 5353 * 2 * 4 / 9;

        printf("Int: %i\n", a);
    }

    return 0;
}
