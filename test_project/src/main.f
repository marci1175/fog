import printf(str: string, res: int): int;
import getchar(): int;
import gets(buf: string): int;

function main(): int {
    string buf = "0000000000000000000";

    loop {
        int a = 0;
        a = 23;
        int a = 2;
        a = 3;

        printf("User input: %i\n", a);
    }

    return 0;
}