import printf(str: string): int;

struct kg {
    inner: float,
}

struct marci {
    suly: kg,
}

function asd(a: int, b: float): int {
    return 2;
}

function main(): int {
    if {
        return 0 == 2;
    } {
        printf("Hello");
    }

    return 0;
}