[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
codegen = { path = "../codegen" }
parser = { path = "../parser" }
analyzer = { path = "../analyzer" }
serde = { version = "1.0.228", features = ["derive"] }